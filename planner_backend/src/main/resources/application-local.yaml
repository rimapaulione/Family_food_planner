spring:
  docker:
    compose:
      enabled: false

  datasource:
    url: jdbc:h2:mem:planner
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

  h2:
    console:
      enabled: true