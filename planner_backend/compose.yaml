services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=family_food_planner'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
